document.addEventListener('DOMContentLoaded',()=>{document.getElementById('userInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMessage();}});});
async function sendMessage(){const inp=document.getElementById('userInput');const msg=inp.value.trim();if(!msg)return;setEnabled(false);addBubble(msg,'user');inp.value='';const typing=addTyping();try{const res=await fetch('/Chat/SendMessage',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:msg})});const data=await res.json();typing.remove();if(!res.ok){addBubble(data.reply||"Sorry, I couldn't get a response right now.",'bot',true);return;}addBubble(data.reply||"Sorry, I couldn't understand the response.",'bot');}catch(err){typing.remove();addBubble("Sorry, I couldn't get a response right now.",'bot',true);}finally{setEnabled(true);inp.focus();}}
function addBubble(text,sender,isErr=false){const w=document.createElement('div');w.className='message '+(sender==='user'?'user-message':'bot-message');const b=document.createElement('div');b.className='bubble'+(isErr?' err-bubble':'');b.textContent=text;w.appendChild(b);document.getElementById('chatMessages').appendChild(w);scroll();}
function addTyping(){const w=document.createElement('div');w.className='message bot-message';const b=document.createElement('div');b.className='bubble typing-bubble';b.textContent='Ojaly is typing...';w.appendChild(b);document.getElementById('chatMessages').appendChild(w);scroll();return w;}
function scroll(){const el=document.getElementById('chatMessages');el.scrollTop=el.scrollHeight;}
function setEnabled(on){document.getElementById('userInput').disabled=!on;document.getElementById('sendBtn').disabled=!on;}
